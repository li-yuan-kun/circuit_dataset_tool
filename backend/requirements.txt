# Runtime dependencies for Circuit Dataset Tool backend (v0.3+)
# Keep this file minimal and stable; add dev/test deps in backend/tests requirements if needed.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.25,<1.0

# Pydantic v2 stack
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# For multipart/form-data (POST /dataset/save)
python-multipart>=0.0.7

# Core numeric + image IO
numpy>=1.24,<3.0
Pillow>=10.0

# Optional (lightweight): used if you later add graph-based checks/routing
networkx>=3.2
